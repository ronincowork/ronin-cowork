# SESSION_MACROS — the owner's `+name:` workflows

When a line of the form `+<name>: <arguments>` (also `<name>:`, `/<name>`, or "run the
`<name>` macro") lands in your tile, compile it first — `tejun <name>` — then execute the
compiled recipe in order and report what it asks for. Never substitute a remembered or
similar-sounding workflow. `tejun` alone lists the macros that exist.

The macros previewed on this tile's macro button, generated at your birth from the live catalog:

<!-- ACTIVE_SESSION_MACROS:START -->
- (generated at birth)
<!-- ACTIVE_SESSION_MACROS:END -->
