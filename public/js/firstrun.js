/* The public entry point keeps its old name; the product surface is cowork_setup. */
import { buildFirstRun as buildCoworkSetup } from './cowork-setup.js';

export const buildFirstRun = buildCoworkSetup;
